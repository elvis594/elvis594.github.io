{
  "name": "github-personal-page",
  "version": "1.0.0",
  "description": "A static personal page built with React, Next.js and Vite",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "deploy": "rm -rf docs && mkdir docs && cp -r out/* docs/ && touch docs/.nojekyll && git add docs && git commit -m \"deploy\" || true && git push",
    "start": "next start",
    "export": "next build",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}